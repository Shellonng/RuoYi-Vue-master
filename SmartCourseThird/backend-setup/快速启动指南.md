# 评估专家 - 快速启动指南

## 🎯 启动前准备清单

在开始之前，确保你的开发环境已安装以下工具：

- [ ] **JDK 17+**（推荐 JDK 17 或 JDK 21）
- [ ] **Maven 3.6+**（或使用IDEA内置的Maven）
- [ ] **MySQL 8.0+**（云服务器或本地）
- [ ] **IntelliJ IDEA**（推荐使用Ultimate版）
- [ ] **Git**
- [ ] **Postman** 或 **Apifox**（接口测试）

---

## 一、环境安装（已安装可跳过）

### 1.1 安装 JDK 17

**Windows**:
1. 下载：https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html
2. 安装后配置环境变量：
   - `JAVA_HOME` = `C:\Program Files\Java\jdk-17`
   - 将 `%JAVA_HOME%\bin` 添加到 `PATH`
3. 验证：
   ```bash
   java -version
   # 应显示：java version "17.x.x"
   ```

**Mac**:
```bash
# 使用Homebrew
brew install openjdk@17

# 验证
java -version
```

### 1.2 安装 Maven

**Windows**:
1. 下载：https://maven.apache.org/download.cgi
2. 解压到 `C:\Program Files\Apache\Maven`
3. 配置环境变量：
   - `MAVEN_HOME` = `C:\Program Files\Apache\Maven`
   - 将 `%MAVEN_HOME%\bin` 添加到 `PATH`
4. 验证：
   ```bash
   mvn -version
   ```

**Mac**:
```bash
brew install maven
mvn -version
```

### 1.3 安装 MySQL

**方式1：云服务器（推荐）**
- 使用阿里云、腾讯云等提供的云数据库
- 优点：团队共享、稳定、免维护

**方式2：本地安装**
```bash
# Windows: 下载安装包
https://dev.mysql.com/downloads/mysql/

# Mac
brew install mysql@8.0
brew services start mysql@8.0
```

**初始化MySQL**:
```bash
# 登录MySQL
mysql -h your-server-ip -u root -p

# 创建数据库
CREATE DATABASE assessment DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建专用用户（推荐）
CREATE USER 'assessment_user'@'%' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON assessment.* TO 'assessment_user'@'%';
FLUSH PRIVILEGES;
```

---

## 二、项目初始化（从零开始）

### 2.1 方式1：使用 Spring Initializr（推荐新手）

1. **打开浏览器访问**: https://start.spring.io/

2. **配置项目**:
   - **Project**: Maven
   - **Language**: Java
   - **Spring Boot**: 3.2.0
   - **Project Metadata**:
     - Group: `com.assessment`
     - Artifact: `assessment-expert`
     - Name: `assessment-expert`
     - Package name: `com.assessment`
     - Packaging: `Jar`
     - Java: `17`

3. **添加依赖**（点击 ADD DEPENDENCIES）:
   - Spring Web
   - Spring Data JPA
   - MySQL Driver
   - Validation
   - Lombok
   - Spring Boot DevTools

4. **点击 GENERATE** 下载项目压缩包

5. **解压并用IDEA打开**

### 2.2 方式2：使用 IDEA 直接创建

1. **打开IDEA** → New Project → Spring Initializr
2. **填写项目信息**（同上）
3. **选择依赖**（同上）
4. **点击 Create**

### 2.3 完善项目配置

#### Step 1: 修改 `pom.xml`

将我提供的 `pom.xml.template` 内容复制到你的 `pom.xml`

```bash
# 在项目根目录执行
mvn clean install
```

如果下载依赖很慢，配置国内镜像：

**修改 Maven 配置文件** (`~/.m2/settings.xml` 或 `Maven安装目录/conf/settings.xml`)：

```xml
<mirrors>
    <mirror>
        <id>aliyun</id>
        <mirrorOf>central</mirrorOf>
        <name>Aliyun Maven</name>
        <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
</mirrors>
```

#### Step 2: 创建目录结构

在 `src/main/java/com/assessment/` 下创建以下包：

```
com.assessment/
├── config/
├── controller/
├── service/
│   └── impl/
├── mapper/
├── entity/
├── dto/
│   ├── request/
│   └── response/
├── vo/
├── common/
├── exception/
├── util/
├── client/
├── interceptor/
└── validator/
```

**快捷方式（IDEA）**:
右键 `com.assessment` → New → Package → 输入包名（如 `controller`）

#### Step 3: 配置 `application.yml`

将我提供的 `application.yml.template` 复制到 `src/main/resources/application.yml`

**修改数据库配置**：

```yaml
spring:
  datasource:
    url: jdbc:mysql://YOUR_SERVER_IP:3306/assessment?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: your_username
    password: your_password
```

**创建环境特定配置**：

`application-dev.yml` (开发环境):
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/assessment?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456

logging:
  level:
    com.assessment: DEBUG
```

`application-prod.yml` (生产环境):
```yaml
spring:
  datasource:
    url: jdbc:mysql://your-cloud-server:3306/assessment?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=true
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    com.assessment: INFO
```

#### Step 4: 初始化数据库

```bash
# 登录MySQL
mysql -h your-server-ip -u root -p

# 执行初始化脚本
source /path/to/init-database.sql

# 或直接复制SQL内容执行
```

---

## 三、编写第一个接口（Hello World）

### 3.1 创建启动类

`src/main/java/com/assessment/AssessmentApplication.java`:

```java
package com.assessment;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AssessmentApplication {
    public static void main(String[] args) {
        SpringApplication.run(AssessmentApplication.class, args);
    }
}
```

### 3.2 创建测试Controller

`src/main/java/com/assessment/controller/TestController.java`:

```java
package com.assessment.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.HashMap;
import java.util.Map;

@RestController
@RequestMapping("/api/v1/test")
public class TestController {

    @GetMapping("/hello")
    public Map<String, Object> hello() {
        Map<String, Object> result = new HashMap<>();
        result.put("code", 200);
        result.put("message", "Hello, Assessment Expert!");
        result.put("timestamp", System.currentTimeMillis());
        return result;
    }

    @GetMapping("/db")
    public Map<String, Object> testDatabase() {
        Map<String, Object> result = new HashMap<>();
        // TODO: 后续添加数据库测试逻辑
        result.put("code", 200);
        result.put("message", "Database connection will be tested here");
        return result;
    }
}
```

### 3.3 启动项目

**方式1：IDEA 启动**
- 打开 `AssessmentApplication.java`
- 点击左侧的绿色三角形 ▶️ → Run

**方式2：命令行启动**
```bash
# 方式1：使用Maven
mvn spring-boot:run

# 方式2：打包后运行
mvn clean package
java -jar target/assessment-expert-1.0.0.jar
```

**查看启动日志**：

```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.2.0)

...
2025-11-17 10:00:00.000  INFO 12345 --- [main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port(s): 8080 (http)
2025-11-17 10:00:00.000  INFO 12345 --- [main] c.a.AssessmentApplication                : Started AssessmentApplication in 3.456 seconds
```

### 3.4 测试接口

**浏览器访问**:
```
http://localhost:8080/api/v1/test/hello
```

**预期返回**:
```json
{
  "code": 200,
  "message": "Hello, Assessment Expert!",
  "timestamp": 1699632000000
}
```

**使用 Postman**:
- 创建新请求：GET `http://localhost:8080/api/v1/test/hello`
- 点击 Send
- 查看响应

---

## 四、核心代码编写（按顺序）

### 4.1 创建公共类

#### 1. 统一返回结果 `Result.java`

复制我之前提供的 `核心代码示例.md` 中的 `Result.java`

#### 2. 业务异常 `BusinessException.java`

复制 `核心代码示例.md` 中的 `BusinessException.java`

#### 3. 全局异常处理 `GlobalExceptionHandler.java`

复制 `核心代码示例.md` 中的 `GlobalExceptionHandler.java`

### 4.2 创建实体类（Entity）

`src/main/java/com/assessment/entity/Grade.java`:

复制 `核心代码示例.md` 中的 `Grade.java`

### 4.3 创建 Mapper

`src/main/java/com/assessment/mapper/GradeMapper.java`:

复制 `核心代码示例.md` 中的 `GradeMapper.java`

**配置MyBatis Plus**:

`src/main/java/com/assessment/config/MyBatisPlusConfig.java`:

```java
package com.assessment.config;

import com.baomidou.mybatisplus.annotation.DbType;
import com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor;
import com.baomidou.mybatisplus.extension.plugins.inner.PaginationInnerInterceptor;
import org.mybatis.spring.annotation.MapperScan;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
@MapperScan("com.assessment.mapper")
public class MyBatisPlusConfig {

    /**
     * 分页插件
     */
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}
```

### 4.4 创建 Service

`src/main/java/com/assessment/service/IGradeService.java`:

```java
package com.assessment.service;

import com.assessment.common.PageResult;
import com.assessment.dto.request.GradeCreateRequest;
import com.assessment.dto.response.GradeResponse;

public interface IGradeService {
    PageResult<GradeResponse> getStudentGrades(Long studentId, Long courseId, String type, Integer page, Integer pageSize);
    Long createGrade(GradeCreateRequest request);
    void updateGrade(Long id, GradeCreateRequest request);
    void deleteGrade(Long id);
}
```

`src/main/java/com/assessment/service/impl/GradeServiceImpl.java`:

复制 `核心代码示例.md` 中的 `GradeServiceImpl.java`

### 4.5 创建 Controller

`src/main/java/com/assessment/controller/GradeController.java`:

复制 `核心代码示例.md` 中的 `GradeController.java`

---

## 五、配置 Swagger（API 文档）

### 5.1 创建 Swagger 配置

`src/main/java/com/assessment/config/SwaggerConfig.java`:

```java
package com.assessment.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Contact;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {

    @Bean
    public OpenAPI customOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("评估专家 API 文档")
                        .description("智能化评估与学情分析引擎接口文档")
                        .version("v1.0.0")
                        .contact(new Contact()
                                .name("开发团队")
                                .email("team@example.com")));
    }
}
```

### 5.2 访问 Swagger UI

启动项目后访问：

```
http://localhost:8080/api/swagger-ui/index.html
```

---

## 六、测试接口

### 6.1 使用 Swagger 测试

1. 打开 Swagger UI
2. 找到 `GradeController`
3. 展开 `POST /api/v1/grades`
4. 点击 "Try it out"
5. 填写请求体：
   ```json
   {
     "studentId": 1,
     "courseId": 1,
     "score": 85.5,
     "totalScore": 100,
     "type": "exam"
   }
   ```
6. 点击 "Execute"
7. 查看响应

### 6.2 使用 Postman 测试

**测试添加成绩**:
```
POST http://localhost:8080/api/v1/grades
Content-Type: application/json

{
  "studentId": 1,
  "courseId": 1,
  "score": 85.5,
  "totalScore": 100,
  "type": "exam"
}
```

**测试查询成绩**:
```
GET http://localhost:8080/api/v1/grades/student/1?page=1&pageSize=20
```

---

## 七、常见问题解决

### 7.1 启动失败

**问题1：端口被占用**
```
Error: Port 8080 is already in use
```

**解决方案**:
```yaml
# 修改 application.yml
server:
  port: 8081  # 改成其他端口
```

**问题2：数据库连接失败**
```
CommunicationsException: Communications link failure
```

**解决方案**:
1. 检查数据库是否启动
2. 检查IP、端口、用户名、密码是否正确
3. 检查防火墙设置（云服务器需开放3306端口）
4. 测试连接：
   ```bash
   mysql -h your-server-ip -u username -p
   ```

**问题3：依赖下载失败**
```
Failed to read artifact descriptor
```

**解决方案**:
1. 配置阿里云镜像（见上文）
2. 删除 `~/.m2/repository` 重新下载
3. 检查网络连接

### 7.2 运行时错误

**问题1：表不存在**
```
Table 'assessment.grade' doesn't exist
```

**解决方案**:
执行数据库初始化脚本 `init-database.sql`

**问题2：空指针异常**
```
NullPointerException
```

**解决方案**:
1. 检查数据库是否有数据
2. 添加空指针检查
3. 查看日志定位问题

---

## 八、Git 仓库初始化

### 8.1 创建 `.gitignore`

在项目根目录创建 `.gitignore`:

```gitignore
# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup

# IDEA
.idea/
*.iml
*.iws
*.ipr

# Logs
logs/
*.log

# 临时文件
*.tmp
*.bak

# 系统文件
.DS_Store
Thumbs.db

# 配置文件（包含敏感信息）
application-prod.yml
application-local.yml

# 上传文件
uploads/
```

### 8.2 初始化 Git

```bash
# 初始化仓库
git init

# 添加所有文件
git add .

# 首次提交
git commit -m "init: 项目初始化"

# 关联远程仓库
git remote add origin https://github.com/your-team/assessment-expert.git

# 推送到远程
git push -u origin main
```

---

## 九、团队协作开始

### 9.1 分支策略

```bash
# 创建develop分支
git checkout -b develop
git push -u origin develop

# 团队成员拉取代码
git clone https://github.com/your-team/assessment-expert.git
cd assessment-expert
git checkout develop
```

### 9.2 功能开发流程

```bash
# 1. 更新develop
git checkout develop
git pull origin develop

# 2. 创建功能分支
git checkout -b feature/grade-management

# 3. 开发并提交
git add .
git commit -m "feat(grade): 实现成绩管理功能"

# 4. 推送
git push origin feature/grade-management

# 5. 创建 Pull Request（在GitHub/GitLab上操作）
```

---

## 十、下一步

完成以上步骤后，你应该：

- [ ] 项目能正常启动
- [ ] 能访问 Swagger UI
- [ ] 能调用测试接口
- [ ] 数据库连接成功
- [ ] Git 仓库创建完成

**接下来的任务**:

1. **Day 1-3**：按照开发计划完成基础模块
2. **Day 4-8**：实现核心功能（成绩、题库、分析、AI批改）
3. **Day 9-13**：联调测试
4. **Day 14-15**：准备交付

---

## 📚 参考资料

- [Spring Boot 官方文档](https://spring.io/projects/spring-boot)
- [MyBatis Plus 官方文档](https://baomidou.com/)
- [Swagger 文档](https://swagger.io/)
- [阿里巴巴Java开发手册](https://github.com/alibaba/p3c)

---

## 🆘 遇到问题？

1. **查看日志**: `logs/assessment-expert.log`
2. **Google 搜索错误信息**
3. **团队讨论**: 在微信群或钉钉群求助
4. **Stack Overflow**: https://stackoverflow.com/

祝开发顺利！🎉
