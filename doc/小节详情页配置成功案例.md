# 若依框架动态路由配置实战案例

> 本文档记录了在若依(RuoYi-Vue)框架中配置小节详情页的完整过程，包括遇到的问题和解决方案。

## 📋 项目背景

需要在课程管理系统中添加小节详情页面，允许用户从课程详情页点击小节标题跳转到小节详情页进行学习。

## 🎯 最终实现效果

- 课程列表页 → 课程详情页 → 小节详情页
- 完整路由：`/course` → `/detail/30` → `/section/30/5`
- 所有页面在框架布局内显示（保留侧边栏和导航栏）

---

## 📊 数据库路由结构

根据实际数据库 `sys_menu` 表的配置：

```
根路径 (parent_id=0)
├── course (课程管理, menu_id=2000)
│   └── course/index (组件路径)
├── detail/:id (课程详情, menu_id=5009)
│   └── course/detail (组件路径)
└── section/:courseId/:sectionId (小节详情, menu_id=5010)
    └── course/section (组件路径)
```

**关键发现：** 
- 课程详情和小节详情都是**根路径下的同级路由**（parent_id=0）
- 不是父子关系，而是平级关系
- 这与通常的层级结构不同，需要特别注意

---

## 🔧 实施步骤

### 第一步：创建前端组件

创建文件：`ruoyi-ui/src/views/course/section.vue`

```vue
<template>
  <div class="section-detail-page">
    <!-- 视频播放区 -->
    <!-- 知识点展示 -->
    <!-- 讨论区 -->
    <!-- 笔记功能 -->
  </div>
</template>

<script>
export default {
  name: "SectionDetail",
  data() {
    return {
      courseId: null,
      sectionId: null,
      // ...其他数据
    };
  },
  created() {
    // 从路由参数获取ID
    this.courseId = this.$route.params.courseId;
    this.sectionId = this.$route.params.sectionId;
  }
};
</script>
```

**关键点：**
- 组件名称必须与数据库 `component` 字段对应
- 组件路径：`course/section` → `views/course/section.vue`

---

### 第二步：配置数据库菜单

执行SQL脚本插入菜单记录：

```sql
-- 插入小节详情页菜单
INSERT INTO sys_menu (
    menu_name,
    parent_id,      -- ★ 设置为 0，与课程详情同级
    order_num,
    path,           -- ★ 动态路由参数
    component,      -- ★ 对应组件路径
    is_frame,
    is_cache,
    menu_type,
    visible,        -- ★ '1' 表示隐藏
    status,
    perms,
    icon,
    create_by,
    create_time,
    remark
) VALUES (
    '小节详情',
    0,              -- 与课程详情同级
    3,
    'section/:courseId/:sectionId',  -- 动态参数
    'course/section',                -- 组件路径
    1,
    0,
    'C',
    '1',            -- 隐藏菜单
    '0',
    'course:section:query',
    '',
    'admin',
    NOW(),
    '小节详情页面，从课程详情页跳转'
);
```

**关键字段说明：**

| 字段 | 值 | 说明 |
|-----|---|------|
| `parent_id` | `0` | **与课程详情同级，不是子菜单** |
| `path` | `section/:courseId/:sectionId` | 动态路由，支持多个参数 |
| `component` | `course/section` | 对应 `views/course/section.vue` |
| `visible` | `'1'` | 隐藏菜单，不在侧边栏显示 |
| `menu_type` | `'C'` | 菜单类型（C=菜单） |

---

### 第三步：配置菜单权限

执行以下SQL为角色分配权限：

```sql
-- 为管理员角色添加小节详情菜单权限
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT 
    1,  -- 管理员角色ID
    menu_id
FROM sys_menu
WHERE menu_name = '小节详情'
  AND NOT EXISTS (
    SELECT 1 FROM sys_role_menu 
    WHERE role_id = 1 
      AND menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情')
  );

-- 批量为所有有课程详情权限的角色添加小节详情权限
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT DISTINCT
    rm.role_id,
    (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情')
FROM sys_role_menu rm
WHERE rm.menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '课程详情')
  AND NOT EXISTS (
    SELECT 1 FROM sys_role_menu rm2
    WHERE rm2.role_id = rm.role_id
      AND rm2.menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情')
  );
```

**权限配置原则：**
- 有课程详情权限的角色，应该也有小节详情权限
- 使用 `NOT EXISTS` 避免重复插入

---

### 第四步：实现页面跳转

在 `detail.vue` 中添加跳转方法：

```javascript
/** 跳转到小节详情页 */
goToSectionDetail(section) {
  // 根据数据库配置: section/:courseId/:sectionId
  this.$router.push({
    path: `/section/${this.courseId}/${section.id}`
  });
}
```

**模板绑定：**

```vue
<div class="section-info" @click="goToSectionDetail(section)" style="cursor: pointer;">
  <h4 class="section-title">{{ section.title }}</h4>
</div>
```

**CSS悬停效果：**

```scss
.section-info {
  flex: 1;
  transition: all 0.3s;

  &:hover {
    .section-title {
      color: #409eff;  // 鼠标悬停变蓝
    }
  }
}
```

---

### 第五步：重启服务并测试

1. **重启后端服务**（必须！让新菜单生效）
2. **清除浏览器缓存**
3. **重新登录系统**
4. 测试跳转流程

---

## ⚠️ 常见问题与解决方案

### 问题1：getRouters返回的JSON中没有小节详情菜单

**原因：** 权限未配置

**排查步骤：**

```sql
-- 1. 确认菜单已插入
SELECT menu_id, menu_name, path FROM sys_menu WHERE menu_name = '小节详情';

-- 2. 检查角色权限
SELECT r.role_id, r.role_name
FROM sys_role r
INNER JOIN sys_role_menu rm ON r.role_id = rm.role_id
WHERE rm.menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情');

-- 3. 检查当前用户权限（替换user_id=1）
SELECT COUNT(*) AS has_permission
FROM sys_user_role ur
INNER JOIN sys_role_menu rm ON ur.role_id = rm.role_id
WHERE ur.user_id = 1
  AND rm.menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情');
```

**解决方案：** 执行第三步的权限配置SQL

---

### 问题2：页面显示404

**可能原因：**
1. 组件文件路径不正确
2. 组件名称与路由不匹配
3. 后端服务未重启

**检查清单：**
- [ ] 文件是否在 `ruoyi-ui/src/views/course/section.vue`
- [ ] 数据库 `component` 字段是否为 `course/section`
- [ ] 是否重启了后端服务
- [ ] 是否清除缓存并重新登录

---

### 问题3：页面没有侧边栏

**原因：** `visible` 字段设置错误

**检查：**

```sql
SELECT visible FROM sys_menu WHERE menu_name = '小节详情';
```

**应该返回：** `'1'` （字符串1，不是数字1）

---

### 问题4：路由参数获取不到

**错误示例：**

```javascript
// ❌ 错误：路径不匹配数据库配置
this.$router.push(`/course/section/${courseId}/${sectionId}`);
```

**正确示例：**

```javascript
// ✅ 正确：与数据库 path 字段一致
this.$router.push(`/section/${courseId}/${sectionId}`);
```

**获取参数：**

```javascript
// 在目标页面中
this.$route.params.courseId    // 获取课程ID
this.$route.params.sectionId   // 获取小节ID
```

---

## 📝 核心经验总结

### 1. 理解若依的动态路由机制

**后端处理流程：**

```
用户登录 
  ↓
查询用户角色权限
  ↓
从 sys_menu 表查询有权限的菜单
  ↓
构建菜单树（根据 parent_id）
  ↓
返回JSON给前端
  ↓
前端动态添加路由（addRoutes）
```

**前端处理流程：**

```javascript
// store/modules/permission.js
GenerateRoutes({ commit }) {
  getRouters().then(res => {
    const routes = filterAsyncRouter(res.data)
    router.addRoutes(routes)  // 动态添加
  })
}
```

---

### 2. 数据库字段与路由的映射关系

| 数据库字段 | 前端对应 | 示例 |
|-----------|---------|------|
| `path` | 路由路径 | `section/:courseId/:sectionId` |
| `component` | 组件路径 | `course/section` → `views/course/section.vue` |
| `parent_id` | 路由嵌套 | `0` = 根路径 |
| `visible` | 侧边栏显示 | `'0'`=显示，`'1'`=隐藏 |
| `perms` | 权限标识 | `course:section:query` |

---

### 3. 路由路径的设计原则

**平级路由：**
```
parent_id = 0
path = detail/:id           → /detail/30
path = section/:courseId    → /section/30
```

**嵌套路由：**
```
parent_id = 2000 (课程管理)
path = detail/:id           → /course/detail/30
path = section/:courseId    → /course/section/30
```

**本案例采用平级路由设计：**
- ✅ 优点：路径简洁，层级清晰
- ✅ 适用：详情页不需要在父页面菜单中显示
- ⚠️ 注意：需要配置权限，否则无法访问

---

### 4. 动态路由参数的最佳实践

**单个参数：**
```javascript
// 数据库配置
path: 'detail/:id'

// 跳转
this.$router.push(`/detail/${id}`)

// 获取
this.$route.params.id
```

**多个参数：**
```javascript
// 数据库配置
path: 'section/:courseId/:sectionId'

// 跳转
this.$router.push(`/section/${courseId}/${sectionId}`)

// 获取
this.$route.params.courseId
this.$route.params.sectionId
```

**命名路由（推荐）：**
```javascript
// 跳转（更清晰）
this.$router.push({
  name: 'SectionDetail',
  params: { 
    courseId: courseId, 
    sectionId: sectionId 
  }
})
```

---

### 5. 权限配置的注意事项

**检查权限的三个层面：**

1. **菜单是否存在**
   ```sql
   SELECT * FROM sys_menu WHERE menu_name = '小节详情';
   ```

2. **角色是否有权限**
   ```sql
   SELECT * FROM sys_role_menu WHERE menu_id = ?;
   ```

3. **用户是否属于该角色**
   ```sql
   SELECT * FROM sys_user_role WHERE user_id = ?;
   ```

**权限继承原则：**
- 父菜单有权限 → 子菜单也应该有权限
- 详情页权限 → 详情页内的子功能都应该有权限

---

## 🎨 完整代码示例

### 数据库配置（SQL）

```sql
-- 1. 插入菜单
INSERT INTO sys_menu (
    menu_name, parent_id, order_num, path, component,
    is_frame, is_cache, menu_type, visible, status, perms
) VALUES (
    '小节详情', 0, 3, 'section/:courseId/:sectionId', 'course/section',
    1, 0, 'C', '1', '0', 'course:section:query'
);

-- 2. 配置权限（为所有有课程详情权限的角色添加）
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT DISTINCT rm.role_id, 
       (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情')
FROM sys_role_menu rm
WHERE rm.menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '课程详情')
  AND NOT EXISTS (
    SELECT 1 FROM sys_role_menu rm2
    WHERE rm2.role_id = rm.role_id
      AND rm2.menu_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '小节详情')
  );
```

---

### 前端跳转（detail.vue）

```vue
<template>
  <div class="section-info" @click="goToSectionDetail(section)">
    <h4 class="section-title">{{ section.title }}</h4>
  </div>
</template>

<script>
export default {
  data() {
    return {
      courseId: null  // 从路由参数获取
    };
  },
  methods: {
    goToSectionDetail(section) {
      this.$router.push(`/section/${this.courseId}/${section.id}`);
    }
  }
};
</script>

<style scoped>
.section-info {
  cursor: pointer;
  transition: all 0.3s;
}

.section-info:hover .section-title {
  color: #409eff;
}
</style>
```

---

### 目标页面（section.vue）

```vue
<script>
export default {
  name: "SectionDetail",
  data() {
    return {
      courseId: null,
      sectionId: null,
      sectionInfo: {}
    };
  },
  created() {
    // 获取路由参数
    this.courseId = this.$route.params.courseId;
    this.sectionId = this.$route.params.sectionId;
    
    // 加载数据
    this.loadData();
  },
  methods: {
    loadData() {
      // 调用API获取小节详情
      console.log(`加载课程${this.courseId}的小节${this.sectionId}`);
    }
  }
};
</script>
```

---

## 🔍 调试技巧

### 1. 查看动态路由是否加载

在浏览器控制台执行：

```javascript
// 查看所有路由
console.log(this.$router.options.routes)

// 查看当前路由
console.log(this.$route)

// 查看路由参数
console.log(this.$route.params)
```

---

### 2. 查看用户菜单权限

在浏览器控制台执行：

```javascript
// 查看用户菜单数据
console.log(this.$store.state.permission.sidebarRouters)

// 查看用户权限
console.log(this.$store.state.user.permissions)
```

---

### 3. 后端API测试

使用浏览器或Postman测试：

```
GET /getRouters
Headers: 
  Authorization: Bearer <token>

响应示例：
{
  "code": 200,
  "data": [
    {
      "path": "section/:courseId/:sectionId",
      "component": "course/section",
      "hidden": true,
      "meta": { "title": "小节详情" }
    }
  ]
}
```

---

## 📚 相关文档链接

- [若依官方文档 - 菜单管理](http://doc.ruoyi.vip/ruoyi-vue/document/qlsc.html#菜单管理)
- [Vue Router - 动态路由匹配](https://v3.router.vuejs.org/zh/guide/essentials/dynamic-matching.html)
- [若依框架源码 - 权限路由](https://gitee.com/y_project/RuoYi-Vue/blob/master/ruoyi-ui/src/store/modules/permission.js)

---

## ✅ 测试清单

配置完成后，按以下步骤验证：

- [ ] 执行SQL脚本，菜单已插入
- [ ] 执行权限配置SQL
- [ ] 重启后端服务
- [ ] 清除浏览器缓存
- [ ] 重新登录系统
- [ ] 左侧菜单**不显示**"小节详情"
- [ ] 进入课程详情页
- [ ] 点击小节标题，能正常跳转
- [ ] 小节详情页正常显示
- [ ] 页面保留侧边栏和顶部导航
- [ ] URL为 `/section/30/5` 格式
- [ ] 刷新页面，详情页仍正常显示
- [ ] 浏览器控制台无错误

---

## 🎯 总结

### 成功关键点

1. **理解路由层级关系** - 平级 vs 嵌套
2. **正确配置数据库字段** - path、component、parent_id、visible
3. **配置菜单权限** - sys_role_menu表
4. **前端路径匹配** - 必须与数据库path一致
5. **重启服务** - 每次修改数据库后必须重启

### 最佳实践

- ✅ 详情页使用 `visible='1'` 隐藏
- ✅ 使用动态参数 `:id` 传递数据
- ✅ 前端跳转路径与数据库path完全一致
- ✅ 批量为角色配置权限，避免遗漏
- ✅ 提供完整的调试和测试方法

---

## 📞 问题反馈

如果遇到问题，按以下顺序排查：

1. 检查数据库菜单是否正确插入
2. 检查权限是否正确配置
3. 确认后端服务已重启
4. 确认浏览器缓存已清除并重新登录
5. 使用浏览器控制台查看路由和权限数据
6. 检查前端跳转路径是否与数据库一致

---

**文档版本：** v1.0  
**最后更新：** 2025年11月22日  
**适用版本：** RuoYi-Vue v3.8+
