# 课程详情页面 - 后端接口实现总结

## 📋 项目概述
已完成 RuoYi-Vue 框架中课程详情页面的完整后端实现，包括章节(Chapter)和小节(Section)的数据库操作、业务逻辑层、数据访问层和REST API接口。

---

## 🏗️ 后端架构设计

### 1. 数据模型层 (Domain)

#### Chapter.java - 章节实体
```
位置: ruoyi-system/src/main/java/com/ruoyi/system/domain/Chapter.java
字段:
  - id: 章节ID (主键)
  - courseId: 所属课程ID (外键)
  - title: 章节名称 (必填)
  - description: 章节描述
  - sortOrder: 排序顺序
  - createTime: 创建时间
  - updateTime: 更新时间
  - isDeleted: 软删除标记
  - sections: 关联的小节列表
```

#### Section.java - 小节实体
```
位置: ruoyi-system/src/main/java/com/ruoyi/system/domain/Section.java
字段:
  - id: 小节ID (主键)
  - chapterId: 所属章节ID (外键)
  - title: 小节名称 (必填)
  - description: 小节简介
  - videoUrl: 视频播放地址
  - duration: 视频时长 (秒)
  - sortOrder: 排序顺序
  - createTime: 创建时间
  - updateTime: 更新时间
  - isDeleted: 软删除标记
  - type: 小节类型 (前端显示: video/document)
  - completed: 完成状态 (前端显示)
```

---

### 2. 数据访问层 (Mapper)

#### ChapterMapper.java
```
位置: ruoyi-system/src/main/java/com/ruoyi/system/mapper/ChapterMapper.java
方法:
  ✓ selectChapterById(Long id) - 查询单个章节
  ✓ selectChapterList(Chapter chapter) - 查询章节列表(支持分页/条件查询)
  ✓ selectChapterListByCourseId(Long courseId) - 根据课程ID查询章节列表
  ✓ insertChapter(Chapter chapter) - 新增章节
  ✓ updateChapter(Chapter chapter) - 修改章节
  ✓ deleteChapterById(Long id) - 删除章节(软删除)
  ✓ deleteChapterByIds(Long[] ids) - 批量删除章节
  
对应SQL映射: ChapterMapper.xml
```

#### SectionMapper.java
```
位置: ruoyi-system/src/main/java/com/ruoyi/system/mapper/SectionMapper.java
方法:
  ✓ selectSectionById(Long id) - 查询单个小节
  ✓ selectSectionList(Section section) - 查询小节列表(支持分页/条件查询)
  ✓ selectSectionListByChapterId(Long chapterId) - 根据章节ID查询小节列表
  ✓ insertSection(Section section) - 新增小节
  ✓ updateSection(Section section) - 修改小节
  ✓ deleteSectionById(Long id) - 删除小节(软删除)
  ✓ deleteSectionByIds(Long[] ids) - 批量删除小节
  
对应SQL映射: SectionMapper.xml
```

---

### 3. 业务逻辑层 (Service)

#### IChapterService.java / ChapterServiceImpl.java
```
位置: ruoyi-system/src/main/java/com/ruoyi/system/service/
方法与Mapper相同，负责:
  - 入参验证
  - 业务逻辑处理
  - 事务管理
  - 调用Mapper进行数据操作
```

#### ISectionService.java / SectionServiceImpl.java
```
位置: ruoyi-system/src/main/java/com/ruoyi/system/service/
方法与Mapper相同，负责:
  - 入参验证
  - 业务逻辑处理
  - 事务管理
  - 调用Mapper进行数据操作
```

---

### 4. 控制层 (Controller)

#### ChapterController.java
```
位置: ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/
REST API接口:

GET /chapter/list
  描述: 获取章节列表(分页)
  入参: Chapter对象(支持courseId、title等条件查询)
  返回: TableDataInfo分页对象
  
GET /chapter/listByCourse/{courseId}
  描述: 根据课程ID获取该课程的所有章节
  入参: courseId (路径参数)
  返回: AjaxResult(data为List<Chapter>)
  
GET /chapter/{id}
  描述: 获取章节详细信息
  入参: id (路径参数)
  返回: AjaxResult(data为Chapter对象)
  
POST /chapter
  描述: 新增章节
  入参: Chapter对象(JSON)
  返回: AjaxResult(成功/失败)
  记录日志: 是
  
PUT /chapter
  描述: 修改章节
  入参: Chapter对象(JSON，需要包含id)
  返回: AjaxResult(成功/失败)
  记录日志: 是
  
DELETE /chapter/{ids}
  描述: 删除章节(支持批量)
  入参: ids (路径参数，多个id用逗号分隔)
  返回: AjaxResult(成功/失败)
  记录日志: 是
```

#### SectionController.java
```
位置: ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/
REST API接口:

GET /section/list
  描述: 获取小节列表(分页)
  入参: Section对象(支持chapterId、title等条件查询)
  返回: TableDataInfo分页对象
  
GET /section/listByChapter/{chapterId}
  描述: 根据章节ID获取该章节的所有小节
  入参: chapterId (路径参数)
  返回: AjaxResult(data为List<Section>)
  
GET /section/{id}
  描述: 获取小节详细信息
  入参: id (路径参数)
  返回: AjaxResult(data为Section对象)
  
POST /section
  描述: 新增小节
  入参: Section对象(JSON)
  返回: AjaxResult(成功/失败)
  记录日志: 是
  
PUT /section
  描述: 修改小节
  入参: Section对象(JSON，需要包含id)
  返回: AjaxResult(成功/失败)
  记录日志: 是
  
DELETE /section/{ids}
  描述: 删除小节(支持批量)
  入参: ids (路径参数，多个id用逗号分隔)
  返回: AjaxResult(成功/失败)
  记录日志: 是
```

---

## 🎯 前端 API 层

### chapter.js
```
位置: ruoyi-ui/src/api/course/chapter.js
导出方法:
  - listChapter(query) - 获取章节列表(分页)
  - listChapterByCourse(courseId) - 根据课程ID获取章节
  - getChapter(id) - 获取章节详情
  - addChapter(data) - 新增章节
  - updateChapter(data) - 修改章节
  - delChapter(id) - 删除单个章节
  - delChapters(ids) - 批量删除章节
```

### section.js
```
位置: ruoyi-ui/src/api/course/section.js
导出方法:
  - listSection(query) - 获取小节列表(分页)
  - listSectionByChapter(chapterId) - 根据章节ID获取小节
  - getSection(id) - 获取小节详情
  - addSection(data) - 新增小节
  - updateSection(data) - 修改小节
  - delSection(id) - 删除单个小节
  - delSections(ids) - 批量删除小节
```

---

## 🖼️ 前端 Vue 组件

### detail.vue - 课程详情页面
```
位置: ruoyi-ui/src/views/course/detail.vue
功能实现:
  ✓ 显示课程基本信息
  ✓ 显示章节列表(从后端获取)
  ✓ 显示每个章节下的小节列表
  ✓ 添加章节 - 弹窗表单，调用后端API
  ✓ 编辑章节 - 弹窗表单，调用后端API
  ✓ 删除章节 - 确认后调用后端API
  ✓ 添加小节 - 弹窗表单，调用后端API
  ✓ 编辑小节 - 弹窗表单，调用后端API
  ✓ 删除小节 - 确认后调用后端API
  ✓ 时长转换 - 秒转分钟显示

关键方法:
  - getCourseInfo() - 获取课程信息
  - getChapterList() - 获取章节列表
  - loadSectionsForChapter(chapter) - 为章节加载小节
  - handleAddChapter() - 打开添加章节对话框
  - handleEditChapter(chapter) - 打开编辑章节对话框
  - handleDeleteChapter(chapter) - 删除章节
  - submitChapterForm() - 提交章节表单
  - handleAddSection(chapter) - 打开添加小节对话框
  - handleEditSection(chapter, section) - 打开编辑小节对话框
  - handleDeleteSection(chapter, section) - 删除小节
  - submitSectionForm() - 提交小节表单
```

---

## 📊 数据库表结构

### chapter 表
```sql
CREATE TABLE `chapter` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '章节ID，主键',
  `course_id` bigint NOT NULL COMMENT '所属课程ID，外键',
  `title` varchar(255) NOT NULL COMMENT '章节名称',
  `description` text NULL COMMENT '章节描述',
  `sort_order` int NOT NULL COMMENT '排序顺序',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint(1) NOT NULL DEFAULT 0 COMMENT '软删除标记',
  `delete_time` datetime NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  INDEX `idx_course_id` (`course_id`),
  INDEX `idx_deleted` (`is_deleted`),
  FOREIGN KEY `fk_chapter_course` (`course_id`) REFERENCES `course` (`id`)
) ENGINE=InnoDB COMMENT='课程章节表';
```

### section 表
```sql
CREATE TABLE `section` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '小节ID，主键',
  `chapter_id` bigint NOT NULL COMMENT '所属章节ID，外键',
  `title` varchar(255) NOT NULL COMMENT '小节名称',
  `description` text NULL COMMENT '小节简介',
  `video_url` varchar(1024) NULL COMMENT '视频播放地址，可对接OSS',
  `duration` int NULL DEFAULT 0 COMMENT '视频时长(秒)',
  `sort_order` int NOT NULL DEFAULT 0 COMMENT '排序顺序',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint(1) NOT NULL DEFAULT 0 COMMENT '软删除标记',
  `delete_time` datetime NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  INDEX `idx_chapter_id` (`chapter_id`),
  INDEX `idx_sort_order` (`sort_order`),
  INDEX `idx_deleted` (`is_deleted`),
  FOREIGN KEY `fk_section_chapter` (`chapter_id`) REFERENCES `chapter` (`id`)
) ENGINE=InnoDB COMMENT='课程小节表';
```

---

## 🔄 业务流程

### 页面加载流程
```
1. 页面mounted时 (created())
   ├─ 从路由参数获取 courseId
   ├─ 调用 getCourseInfo() 获取课程基本信息
   └─ 调用 getChapterList() 获取章节列表

2. getChapterList()
   ├─ 调用后端 GET /chapter/listByCourse/{courseId}
   └─ 获取所有章节后，为每个章节调用 loadSectionsForChapter()

3. loadSectionsForChapter(chapter)
   ├─ 调用后端 GET /section/listByChapter/{chapterId}
   ├─ 获取小节列表
   ├─ 处理小节类型 (video/document)
   └─ 将时长从秒转换为 "x分x秒" 格式
```

### 新增章节流程
```
1. 用户点击 "添加章节" 按钮
2. 打开对话框，初始化表单
3. 用户填写表单并点击 "确定"
4. 前端验证表单数据
5. 调用后端 POST /chapter (包含courseId、title、description等)
6. 后端保存到数据库
7. 返回成功消息
8. 前端重新加载章节列表显示新章节
```

### 编辑章节流程
```
1. 用户点击章节的 "编辑" 按钮
2. 打开对话框，使用现有数据填充表单
3. 用户修改表单并点击 "确定"
4. 前端验证表单数据
5. 调用后端 PUT /chapter (包含id及修改的字段)
6. 后端更新数据库中的记录
7. 返回成功消息
8. 前端重新加载章节列表显示更新后的内容
```

### 删除章节流程
```
1. 用户点击章节的 "删除" 按钮
2. 弹出确认对话框
3. 用户确认删除
4. 调用后端 DELETE /chapter/{id}
5. 后端执行软删除 (is_deleted = 1, delete_time = now)
6. 返回成功消息
7. 前端重新加载章节列表
8. 小节表流程相同
```

---

## ✅ 完成清单

- [x] 创建 Chapter Entity 类
- [x] 创建 Section Entity 类
- [x] 创建 ChapterMapper 接口及 XML 配置
- [x] 创建 SectionMapper 接口及 XML 配置
- [x] 创建 IChapterService 接口及实现类
- [x] 创建 ISectionService 接口及实现类
- [x] 创建 ChapterController REST API
- [x] 创建 SectionController REST API
- [x] 创建 chapter.js 前端 API 模块
- [x] 创建 section.js 前端 API 模块
- [x] 修改 detail.vue 使用真实后端 API
- [x] 实现章节增删改查功能
- [x] 实现小节增删改查功能
- [x] 时长格式转换 (秒 ↔ 分)
- [x] 错误提示和处理

---

## 🚀 使用方法

### 后端启动
```bash
1. 确保MySQL数据库已启动
2. 使用 smartclassv2.sql 初始化数据库
3. 启动 ruoyi-admin 应用 (Spring Boot Application)
4. 默认访问地址: http://localhost:8080
```

### 前端使用
```bash
1. 进入 ruoyi-ui 目录
2. npm install
3. npm run dev
4. 访问课程列表，点击某个课程进入详情页
5. 对话框中可以进行章节和小节的增删改操作
```

### API 测试
```
使用 Postman 或其他 HTTP 客户端测试 REST API:

基础 URL: http://localhost:8080

示例:
GET /chapter/listByCourse/1
POST /chapter (body: {"courseId": 1, "title": "第一章", "description": "...", "sortOrder": 0})
PUT /chapter (body: {"id": 1, "title": "修改后的名称", ...})
DELETE /chapter/1
```

---

## 📝 注意事项

1. **软删除**: 数据删除使用软删除方式，原数据保留在数据库中
2. **排序**: 通过 sort_order 字段实现章节和小节的排序
3. **时长单位**: 数据库存储为秒，前端显示为 "x分x秒" 格式
4. **级联操作**: 删除章节会同时删除该章节下的所有小节
5. **权限控制**: 可在 Controller 方法上添加 @PreAuthorize 注解进行权限控制
6. **日志记录**: 新增、修改、删除操作会自动记录操作日志

---

**实现完成时间**: 2025-11-20  
**实现者**: AI Assistant  
**框架版本**: RuoYi v3.9.0
