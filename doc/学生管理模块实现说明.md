# 学生管理模块实现说明

## 模块概述

学生管理模块包含**选课管理**和**班级管理**两个子模块，完全复用了任务管理（考试管理/作业管理）的界面结构和交互模式。

---

## 📁 文件结构

```
ruoyi-ui/src/views/student/
├── index.vue                    # 主页面（标签页容器）
├── enrollment/
│   └── index.vue               # 选课管理页面
└── class/
    └── index.vue               # 班级管理页面

ruoyi-ui/src/api/system/
├── enrollment.js               # 选课管理API
└── class.js                    # 班级管理API

sql/
└── student_management_menu.sql # 数据库菜单配置脚本
```

---

## 🎯 功能特性

### 选课管理

**字段设计：**
- 学生ID
- 学生姓名
- 所选课程
- 状态（待审核/已选课/已退课）
- 选课时间

**功能操作：**
- ✅ 查询：支持按学生ID、课程、状态筛选
- ✅ 新增：添加新的选课记录
- ✅ 修改：编辑选课信息
- ✅ 删除：删除选课记录
- ✅ 审核：待审核状态可以审核通过
- ✅ 导出：导出选课数据

---

### 班级管理

**字段设计：**
- 班级ID
- 班级名称
- 班级人数
- 班主任
- 状态（正常/停用）
- 创建时间

**功能操作：**
- ✅ 查询：支持按班级名称、状态筛选
- ✅ 新增：创建新班级
- ✅ 修改：编辑班级信息
- ✅ 删除：删除班级
- ✅ 学生列表：查看班级内的学生
- ✅ 导出：导出班级数据

---

## 🚀 部署步骤

### 第一步：执行SQL脚本

连接数据库，执行以下脚本：

```bash
# 使用MySQL客户端或Navicat执行
mysql -u root -p < sql/student_management_menu.sql
```

或在数据库管理工具中执行 `sql/student_management_menu.sql` 文件。

**脚本内容：**
1. 插入"学生管理"主菜单
2. 插入选课和班级的权限按钮
3. 为管理员角色分配权限
4. 验证配置是否成功

---

### 第二步：验证数据库配置

执行验证查询：

```sql
-- 查看菜单结构
SELECT 
    m.menu_id,
    m.menu_name,
    m.parent_id,
    m.path,
    m.component,
    m.menu_type,
    m.perms
FROM sys_menu m
WHERE m.menu_name = '学生管理'
   OR m.parent_id = (SELECT menu_id FROM sys_menu WHERE menu_name = '学生管理')
ORDER BY m.parent_id, m.order_num;
```

**预期结果：**
- 1条主菜单记录（学生管理）
- 10条按钮记录（选课和班级的增删改查导权限）

---

### 第三步：重启后端服务

```bash
# Windows
cd ruoyi-admin
mvn spring-boot:run

# 或使用启动脚本
.\ry.bat
```

**重要：** 必须重启后端，才能加载新的菜单配置！

---

### 第四步：清除缓存并登录

1. **清除浏览器缓存**
   - Chrome: Ctrl + Shift + Delete
   - 或使用隐私模式访问

2. **重新登录系统**
   - 访问 http://localhost:80
   - 使用管理员账号登录

3. **验证菜单显示**
   - 左侧菜单应显示"学生管理"项
   - 图标为 `peoples`（用户组图标）

---

### 第五步：测试功能

#### 测试选课管理
1. 点击"学生管理" → "选课管理"标签
2. 点击"新增"按钮
3. 填写表单：
   - 学生ID: 2021001
   - 所选课程: 选择任意课程
   - 状态: 选择"待审核"
4. 点击"确定"保存
5. 对待审核记录点击"审核通过"
6. 验证状态变更为"已选课"

#### 测试班级管理
1. 切换到"班级管理"标签
2. 点击"新增"按钮
3. 填写表单：
   - 班级名称: 计算机2021级1班
   - 班主任: 选择教师
   - 状态: 正常
4. 点击"确定"保存
5. 点击"学生列表"查看班级学生

---

## 🎨 界面预览

### 选课管理界面

```
┌─────────────────────────────────────────────────────┐
│ 学生管理                                              │
├─────────────────────────────────────────────────────┤
│ [选课管理] [班级管理]                                  │
├─────────────────────────────────────────────────────┤
│ 学生ID: [____] 所选课程: [下拉] 状态: [下拉] [搜索] [重置] │
├─────────────────────────────────────────────────────┤
│ [+新增] [✎修改] [×删除] [↓导出]                       │
├─────────────────────────────────────────────────────┤
│ ☐ 学生ID │ 学生姓名 │ 所选课程 │ 状态 │ 选课时间 │ 操作   │
│ ☐ 2021001│ 张三    │ 计算机... │[待审核]│ 2025-11 │ 修改删除审核│
│ ☐ 2021002│ 李四    │ 计算机... │[已选课]│ 2025-11 │ 修改删除    │
└─────────────────────────────────────────────────────┘
```

### 班级管理界面

```
┌─────────────────────────────────────────────────────┐
│ [选课管理] [班级管理]                                  │
├─────────────────────────────────────────────────────┤
│ 班级名称: [____] 状态: [下拉] [搜索] [重置]            │
├─────────────────────────────────────────────────────┤
│ [+新增] [✎修改] [×删除] [↓导出]                       │
├─────────────────────────────────────────────────────┤
│ ☐ ID │ 班级名称 │ 人数 │ 班主任 │ 状态 │ 创建时间 │ 操作 │
│ ☐ 1  │ 2021级1班│ 45  │ 王老师 │[正常]│ 2025-11 │ 修改删除学生│
│ ☐ 2  │ 2021级2班│ 42  │ 李老师 │[正常]│ 2025-11 │ 修改删除学生│
└─────────────────────────────────────────────────────┘
```

---

## 🔧 后端开发指南

### 需要创建的Java类

#### 1. 实体类

**选课实体 (Enrollment.java)**
```java
package com.ruoyi.system.domain;

public class Enrollment extends BaseEntity {
    private Long id;
    private String studentId;      // 学生ID
    private String studentName;    // 学生姓名
    private Long courseId;         // 课程ID
    private String courseName;     // 课程名称
    private String status;         // 状态：0待审核,1已选课,2已退课
    private Date enrollTime;       // 选课时间
    
    // getter/setter省略
}
```

**班级实体 (Class.java)**
```java
package com.ruoyi.system.domain;

public class Class extends BaseEntity {
    private Long id;
    private String className;      // 班级名称
    private Integer studentCount;  // 学生人数
    private Long teacherId;        // 班主任ID
    private String teacherName;    // 班主任姓名
    private String status;         // 状态：0正常,1停用
    
    // getter/setter省略
}
```

---

#### 2. Mapper接口

**EnrollmentMapper.java**
```java
package com.ruoyi.system.mapper;

import java.util.List;
import com.ruoyi.system.domain.Enrollment;

public interface EnrollmentMapper {
    List<Enrollment> selectEnrollmentList(Enrollment enrollment);
    Enrollment selectEnrollmentById(Long id);
    int insertEnrollment(Enrollment enrollment);
    int updateEnrollment(Enrollment enrollment);
    int deleteEnrollmentById(Long id);
}
```

**ClassMapper.java**
```java
package com.ruoyi.system.mapper;

import java.util.List;
import com.ruoyi.system.domain.Class;

public interface ClassMapper {
    List<Class> selectClassList(Class clazz);
    Class selectClassById(Long id);
    int insertClass(Class clazz);
    int updateClass(Class clazz);
    int deleteClassById(Long id);
}
```

---

#### 3. Service接口

**IEnrollmentService.java**
```java
package com.ruoyi.system.service;

public interface IEnrollmentService {
    List<Enrollment> selectEnrollmentList(Enrollment enrollment);
    Enrollment selectEnrollmentById(Long id);
    int insertEnrollment(Enrollment enrollment);
    int updateEnrollment(Enrollment enrollment);
    int deleteEnrollmentByIds(Long[] ids);
    int approveEnrollment(Long id);
}
```

---

#### 4. Controller

**EnrollmentController.java**
```java
package com.ruoyi.system.controller;

@RestController
@RequestMapping("/system/enrollment")
public class EnrollmentController extends BaseController {
    
    @Autowired
    private IEnrollmentService enrollmentService;
    
    @PreAuthorize("@ss.hasPermi('system:enrollment:list')")
    @GetMapping("/list")
    public TableDataInfo list(Enrollment enrollment) {
        startPage();
        List<Enrollment> list = enrollmentService.selectEnrollmentList(enrollment);
        return getDataTable(list);
    }
    
    @PreAuthorize("@ss.hasPermi('system:enrollment:query')")
    @GetMapping("/{id}")
    public AjaxResult getInfo(@PathVariable Long id) {
        return success(enrollmentService.selectEnrollmentById(id));
    }
    
    @PreAuthorize("@ss.hasPermi('system:enrollment:add')")
    @PostMapping
    public AjaxResult add(@RequestBody Enrollment enrollment) {
        return toAjax(enrollmentService.insertEnrollment(enrollment));
    }
    
    @PreAuthorize("@ss.hasPermi('system:enrollment:edit')")
    @PutMapping
    public AjaxResult edit(@RequestBody Enrollment enrollment) {
        return toAjax(enrollmentService.updateEnrollment(enrollment));
    }
    
    @PreAuthorize("@ss.hasPermi('system:enrollment:remove')")
    @DeleteMapping("/{ids}")
    public AjaxResult remove(@PathVariable Long[] ids) {
        return toAjax(enrollmentService.deleteEnrollmentByIds(ids));
    }
}
```

---

#### 5. Mapper XML

**EnrollmentMapper.xml**
```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.EnrollmentMapper">
    
    <resultMap id="EnrollmentResult" type="Enrollment">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="student_name" property="studentName"/>
        <result column="course_id" property="courseId"/>
        <result column="course_name" property="courseName"/>
        <result column="status" property="status"/>
        <result column="enroll_time" property="enrollTime"/>
    </resultMap>
    
    <select id="selectEnrollmentList" parameterType="Enrollment" resultMap="EnrollmentResult">
        SELECT e.*, c.title AS course_name
        FROM enrollment e
        LEFT JOIN course c ON e.course_id = c.id
        <where>
            <if test="studentId != null and studentId != ''">
                AND e.student_id LIKE CONCAT('%', #{studentId}, '%')
            </if>
            <if test="courseId != null">
                AND e.course_id = #{courseId}
            </if>
            <if test="status != null and status != ''">
                AND e.status = #{status}
            </if>
        </where>
        ORDER BY e.enroll_time DESC
    </select>
    
</mapper>
```

---

## 📊 数据库表设计

### 选课表 (enrollment)

```sql
CREATE TABLE `enrollment` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `student_id` varchar(50) NOT NULL COMMENT '学生ID',
  `student_name` varchar(100) DEFAULT NULL COMMENT '学生姓名',
  `course_id` bigint(20) NOT NULL COMMENT '课程ID',
  `status` char(1) DEFAULT '0' COMMENT '状态：0待审核,1已选课,2已退课',
  `enroll_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '选课时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `create_by` varchar(64) DEFAULT '',
  `update_by` varchar(64) DEFAULT '',
  `remark` varchar(500) DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_student` (`student_id`),
  KEY `idx_course` (`course_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='选课表';
```

### 班级表 (class)

```sql
CREATE TABLE `class` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `class_name` varchar(100) NOT NULL COMMENT '班级名称',
  `student_count` int(11) DEFAULT '0' COMMENT '学生人数',
  `teacher_id` bigint(20) DEFAULT NULL COMMENT '班主任ID',
  `teacher_name` varchar(100) DEFAULT NULL COMMENT '班主任姓名',
  `status` char(1) DEFAULT '0' COMMENT '状态：0正常,1停用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `create_by` varchar(64) DEFAULT '',
  `update_by` varchar(64) DEFAULT '',
  `remark` varchar(500) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级表';
```

---

## ⚠️ 注意事项

### 1. 权限控制

前端使用 `v-hasPermi` 指令控制按钮显示：
```vue
<el-button v-hasPermi="['system:enrollment:add']">新增</el-button>
```

后端使用 `@PreAuthorize` 注解控制接口访问：
```java
@PreAuthorize("@ss.hasPermi('system:enrollment:add')")
```

### 2. API调用

当前前端使用**模拟数据**，后端开发完成后需要：

1. 取消注释真实API调用
2. 删除模拟数据代码
3. 测试接口联调

**示例修改：**
```javascript
// 修改前（模拟数据）
getList() {
  setTimeout(() => {
    this.enrollmentList = [...]; // 模拟数据
  }, 500);
}

// 修改后（真实API）
getList() {
  this.loading = true;
  listEnrollment(this.queryParams).then(response => {
    this.enrollmentList = response.rows;
    this.total = response.total;
    this.loading = false;
  });
}
```

### 3. 数据表创建

执行 SQL 创建 `enrollment` 和 `class` 表后才能正常使用。

---

## 🎯 测试清单

部署完成后，按以下清单验证：

- [ ] 执行 SQL 脚本，菜单已插入
- [ ] 验证数据库权限配置正确
- [ ] 重启后端服务
- [ ] 清除浏览器缓存并重新登录
- [ ] 左侧菜单显示"学生管理"
- [ ] 点击后显示两个标签页
- [ ] 选课管理-新增功能正常
- [ ] 选课管理-修改功能正常
- [ ] 选课管理-删除功能正常
- [ ] 选课管理-审核功能正常
- [ ] 班级管理-新增功能正常
- [ ] 班级管理-修改功能正常
- [ ] 班级管理-删除功能正常
- [ ] 班级管理-学生列表显示正常
- [ ] 导出功能正常（需后端实现）

---

## 📚 相关文档

- [任务管理模块参考](../assignment/)
- [若依权限管理文档](http://doc.ruoyi.vip/)
- [课程详情页动态路由配置](./课程详情页动态路由配置.md)

---

**文档版本：** v1.0  
**最后更新：** 2025年11月25日  
**适用版本：** RuoYi-Vue v3.8+
